#poll mail.ibiblio.org 
#   protocol auto
#   username "deerpig" 
#   password "1zm0alqp"
#   fetchall
#   mda "/usr/bin/procmail -d %T"

#set postmaster "deerpig"
#set bouncemail
#set logfile ~/log/fetchmail.log

poll imap.gmail.com with proto IMAP and options no dns 
user 'brad@chenla.la' there with password 'Twattering0fbardz' is 'deerpig' here 
  nofetchall
  keep ssl
  mda "/usr/bin/procmail -d %T"

 poll imap.gmail.com with proto IMAP and options no dns 
 user 'deerpig@gmail.com' there with password 'cr0nkthat#!up' is 'deerpig' here 
   nofetchall
   keep ssl
   mda "/usr/bin/procmail -d %T"

poll imap.gmail.com with proto IMAP and options no dns 
user 'cas.nakamori@gmail.com' there with password 'Twattering0fbardz' is 'deerpig' here 
  nofetchall
  keep ssl
  mda "/usr/bin/procmail -d %T"

poll mail.kinto-cloud.com with proto IMAP and options no dns 
user 'brad' there with password '1zm0alqp' is 'deerpig' here 
  nofetchall
  keep ssl
  mda "/usr/bin/procmail -d %T"

# poll mail.pathmazing.com with proto IMAP and options no dns 
# user 'brad.collins@pathmazing.com' there with password 'Twattering0fbardz' is 'deerpig' here 
#   fetchall
#   keep ssl
#   mda "/usr/bin/procmail -d %T"

# webmail.ibiblio.org
#poll imap.gmail.com with proto IMAP and options no dns 
#user 'deerpig@ibiblio.org' there with password 'vegetasucks' is 'deerpig' here 
#  nofetchall
#  keep ssl
#  mda "/usr/bin/procmail -d %T"

#fetchall
#no keep
#mda "/usr/bin/procmail -f %F -d %T";
